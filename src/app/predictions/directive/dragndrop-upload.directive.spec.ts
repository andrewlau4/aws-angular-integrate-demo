import { DragndropUploadDirective } from './dragndrop-upload.directive';

describe('DragndropUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new DragndropUploadDirective();
    expect(directive).toBeTruthy();
  });
});
